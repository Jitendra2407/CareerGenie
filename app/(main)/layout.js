import React from "react";

const MainLayout = ({ childeren }) => {
  // Redirect to onboarding
  return <div className="container mx-auto mt-24 mb-20">{childeren}</div>;
};

export default MainLayout;
